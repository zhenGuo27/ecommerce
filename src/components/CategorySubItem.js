const CategorySubItem = (props) => {
  return (
    <li className="level2">
      <a href="#" className="site-nav">
        {props.title}
      </a>
    </li>
  );
};

export default CategorySubItem;
